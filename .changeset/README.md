# Changesets

Changesets describe user-facing changes to published packages. Run `pnpm changeset`, choose the affected package and semantic version bump, then commit the generated Markdown file with the change.

Documentation, tests, CI, and repository maintenance do not need a changeset unless they alter the published package.
