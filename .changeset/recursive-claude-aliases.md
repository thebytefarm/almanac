---
'almanac-md': minor
---

Add CLI-only index filters and targets, standalone `index` and `link` commands, and recursive `CLAUDE.md` and `GEMINI.md` compatibility links for every Git-visible `AGENTS.md`.
